# Student-Management-System
Student Management System is a C++ console-based application that allows users to manage student records efficiently. It supports adding, viewing, searching, updating, and deleting records with proper input validation and file handling for persistent data storage using a simple text-based database.
